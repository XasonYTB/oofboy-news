document.addEventListener("DOMContentLoaded", () => {
  // Simulate fetching subscriber count from an API (for demonstration purposes)
  const subCount = 301;
  document.getElementById('sub-count').textContent = `${subCount} Subs`;
});
